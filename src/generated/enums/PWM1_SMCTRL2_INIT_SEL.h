/**
 * \file
 * \brief Generated by ifgen (3.3.0).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace MIMXRT1176::CM7
{

enum class PWM1_SMCTRL2_INIT_SEL : uint8_t
{
    PWM_X /*!< Local sync (PWM_X) causes initialization. */,
    MASTER_RELOAD =
        1 /*!< Master reload from submodule 0 causes initialization. This
             setting should not be used in submodule 0 as it will force the
             INIT signal to logic 0. The submodule counter will only
             reinitialize when a master reload occurs. */
        ,
    MASTER_SYNC = 2 /*!< Master sync from submodule 0 causes initialization.
                       This setting should not be used in submodule 0 as it
                       will force the INIT signal to logic 0. */
        ,
    EXT_SYNC = 3 /*!< EXT_SYNC causes initialization. */
};
static_assert(sizeof(PWM1_SMCTRL2_INIT_SEL) == 1);

/**
 * Converts PWM1_SMCTRL2_INIT_SEL to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(PWM1_SMCTRL2_INIT_SEL instance)
{
    const char *result = "UNKNOWN PWM1_SMCTRL2_INIT_SEL";

    switch (instance)
    {
    case PWM1_SMCTRL2_INIT_SEL::PWM_X:
        result = "PWM_X";
        break;
    case PWM1_SMCTRL2_INIT_SEL::MASTER_RELOAD:
        result = "MASTER_RELOAD";
        break;
    case PWM1_SMCTRL2_INIT_SEL::MASTER_SYNC:
        result = "MASTER_SYNC";
        break;
    case PWM1_SMCTRL2_INIT_SEL::EXT_SYNC:
        result = "EXT_SYNC";
        break;
    }

    return result;
}

/**
 * Converts a C string to PWM1_SMCTRL2_INIT_SEL.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, PWM1_SMCTRL2_INIT_SEL &output)
{
    bool result = false;

    if ((result = !strncmp(data, "PWM_X", 5)))
    {
        output = PWM1_SMCTRL2_INIT_SEL::PWM_X;
    }
    else if ((result = !strncmp(data, "MASTER_RELOAD", 13)))
    {
        output = PWM1_SMCTRL2_INIT_SEL::MASTER_RELOAD;
    }
    else if ((result = !strncmp(data, "MASTER_SYNC", 11)))
    {
        output = PWM1_SMCTRL2_INIT_SEL::MASTER_SYNC;
    }
    else if ((result = !strncmp(data, "EXT_SYNC", 8)))
    {
        output = PWM1_SMCTRL2_INIT_SEL::EXT_SYNC;
    }

    return result;
}

}; // namespace MIMXRT1176::CM7
