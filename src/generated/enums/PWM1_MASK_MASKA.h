/**
 * \file
 * \brief Generated by ifgen (3.3.0).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace MIMXRT1176::CM7
{

enum class PWM1_MASK_MASKA : uint8_t
{
    NORMAL /*!< PWM_A output normal. */,
    MASKED = 1 /*!< PWM_A output masked. */
};
static_assert(sizeof(PWM1_MASK_MASKA) == 1);

/**
 * Converts PWM1_MASK_MASKA to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(PWM1_MASK_MASKA instance)
{
    const char *result = "UNKNOWN PWM1_MASK_MASKA";

    switch (instance)
    {
    case PWM1_MASK_MASKA::NORMAL:
        result = "NORMAL";
        break;
    case PWM1_MASK_MASKA::MASKED:
        result = "MASKED";
        break;
    }

    return result;
}

/**
 * Converts a C string to PWM1_MASK_MASKA.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, PWM1_MASK_MASKA &output)
{
    bool result = false;

    if ((result = !strncmp(data, "NORMAL", 6)))
    {
        output = PWM1_MASK_MASKA::NORMAL;
    }
    else if ((result = !strncmp(data, "MASKED", 6)))
    {
        output = PWM1_MASK_MASKA::MASKED;
    }

    return result;
}

}; // namespace MIMXRT1176::CM7
