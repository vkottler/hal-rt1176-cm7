/**
 * \file
 * \brief Generated by ifgen (3.3.0).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace MIMXRT1176::CM7
{

enum class PWM1_FCTRL0_FAUTO : uint8_t
{
    MANUAL /*!< Manual fault clearing. PWM outputs disabled by this fault are
              not enabled until FSTS[FFLAGx] is clear at the start of a half
              cycle or full cycle depending the states of FSTS[FHALF] and
              FSTS[FFULL]. If neither FFULL nor FHALF is set, then the fault
              condition cannot be cleared. This is further controlled by
              FCTRL[FSAFE]. */
        ,
    AUTOMATIC =
        1 /*!< Automatic fault clearing. PWM outputs disabled by this fault are
             enabled when FSTS[FFPINx] is clear at the start of a half cycle or
             full cycle depending on the states of FSTS[FHALF] and FSTS[FFULL]
             without regard to the state of FSTS[FFLAGx]. If neither FFULL nor
             FHALF is set, then the fault condition cannot be cleared. */
};
static_assert(sizeof(PWM1_FCTRL0_FAUTO) == 1);

/**
 * Converts PWM1_FCTRL0_FAUTO to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(PWM1_FCTRL0_FAUTO instance)
{
    const char *result = "UNKNOWN PWM1_FCTRL0_FAUTO";

    switch (instance)
    {
    case PWM1_FCTRL0_FAUTO::MANUAL:
        result = "MANUAL";
        break;
    case PWM1_FCTRL0_FAUTO::AUTOMATIC:
        result = "AUTOMATIC";
        break;
    }

    return result;
}

/**
 * Converts a C string to PWM1_FCTRL0_FAUTO.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, PWM1_FCTRL0_FAUTO &output)
{
    bool result = false;

    if ((result = !strncmp(data, "MANUAL", 6)))
    {
        output = PWM1_FCTRL0_FAUTO::MANUAL;
    }
    else if ((result = !strncmp(data, "AUTOMATIC", 9)))
    {
        output = PWM1_FCTRL0_FAUTO::AUTOMATIC;
    }

    return result;
}

}; // namespace MIMXRT1176::CM7
