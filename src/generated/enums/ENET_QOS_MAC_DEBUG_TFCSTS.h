/**
 * \file
 * \brief Generated by ifgen (3.3.0).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace MIMXRT1176::CM7
{

enum class ENET_QOS_MAC_DEBUG_TFCSTS : uint8_t
{
    IDLE /*!< Idle state */,
    WAITING = 1 /*!< Waiting for one of the following: Status of the previous
                   packet OR IPG or back off period to be over */
        ,
    GEN_TX_PAU = 2 /*!< Generating and transmitting a Pause control packet (in
                      full-duplex mode) */
        ,
    TRNSFR = 3 /*!< Transferring input packet for transmission */
};
static_assert(sizeof(ENET_QOS_MAC_DEBUG_TFCSTS) == 1);

/**
 * Converts ENET_QOS_MAC_DEBUG_TFCSTS to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(ENET_QOS_MAC_DEBUG_TFCSTS instance)
{
    const char *result = "UNKNOWN ENET_QOS_MAC_DEBUG_TFCSTS";

    switch (instance)
    {
    case ENET_QOS_MAC_DEBUG_TFCSTS::IDLE:
        result = "IDLE";
        break;
    case ENET_QOS_MAC_DEBUG_TFCSTS::WAITING:
        result = "WAITING";
        break;
    case ENET_QOS_MAC_DEBUG_TFCSTS::GEN_TX_PAU:
        result = "GEN_TX_PAU";
        break;
    case ENET_QOS_MAC_DEBUG_TFCSTS::TRNSFR:
        result = "TRNSFR";
        break;
    }

    return result;
}

/**
 * Converts a C string to ENET_QOS_MAC_DEBUG_TFCSTS.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, ENET_QOS_MAC_DEBUG_TFCSTS &output)
{
    bool result = false;

    if ((result = !strncmp(data, "IDLE", 4)))
    {
        output = ENET_QOS_MAC_DEBUG_TFCSTS::IDLE;
    }
    else if ((result = !strncmp(data, "WAITING", 7)))
    {
        output = ENET_QOS_MAC_DEBUG_TFCSTS::WAITING;
    }
    else if ((result = !strncmp(data, "GEN_TX_PAU", 10)))
    {
        output = ENET_QOS_MAC_DEBUG_TFCSTS::GEN_TX_PAU;
    }
    else if ((result = !strncmp(data, "TRNSFR", 6)))
    {
        output = ENET_QOS_MAC_DEBUG_TFCSTS::TRNSFR;
    }

    return result;
}

}; // namespace MIMXRT1176::CM7
