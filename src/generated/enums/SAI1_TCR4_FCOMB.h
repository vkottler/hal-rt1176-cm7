/**
 * \file
 * \brief Generated by ifgen (3.3.0).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace MIMXRT1176::CM7
{

enum class SAI1_TCR4_FCOMB : uint8_t
{
    DISABLED /*!< FIFO combine mode disabled. */,
    ENABLED_ON_FIFO_READS = 1 /*!< FIFO combine mode enabled on FIFO reads
                                 (from transmit shift registers). */
        ,
    ENABLED_ON_FIFO_WRITES =
        2 /*!< FIFO combine mode enabled on FIFO writes (by software). */,
    ENABLED_ON_FIFO_READS_WRITES =
        3 /*!< FIFO combine mode enabled on FIFO reads (from transmit shift
             registers) and writes (by software). */
};
static_assert(sizeof(SAI1_TCR4_FCOMB) == 1);

/**
 * Converts SAI1_TCR4_FCOMB to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(SAI1_TCR4_FCOMB instance)
{
    const char *result = "UNKNOWN SAI1_TCR4_FCOMB";

    switch (instance)
    {
    case SAI1_TCR4_FCOMB::DISABLED:
        result = "DISABLED";
        break;
    case SAI1_TCR4_FCOMB::ENABLED_ON_FIFO_READS:
        result = "ENABLED_ON_FIFO_READS";
        break;
    case SAI1_TCR4_FCOMB::ENABLED_ON_FIFO_WRITES:
        result = "ENABLED_ON_FIFO_WRITES";
        break;
    case SAI1_TCR4_FCOMB::ENABLED_ON_FIFO_READS_WRITES:
        result = "ENABLED_ON_FIFO_READS_WRITES";
        break;
    }

    return result;
}

/**
 * Converts a C string to SAI1_TCR4_FCOMB.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, SAI1_TCR4_FCOMB &output)
{
    bool result = false;

    if ((result = !strncmp(data, "DISABLED", 8)))
    {
        output = SAI1_TCR4_FCOMB::DISABLED;
    }
    else if ((result = !strncmp(data, "ENABLED_ON_FIFO_READS", 21)))
    {
        output = SAI1_TCR4_FCOMB::ENABLED_ON_FIFO_READS;
    }
    else if ((result = !strncmp(data, "ENABLED_ON_FIFO_WRITES", 22)))
    {
        output = SAI1_TCR4_FCOMB::ENABLED_ON_FIFO_WRITES;
    }
    else if ((result = !strncmp(data, "ENABLED_ON_FIFO_READS_WRITES", 28)))
    {
        output = SAI1_TCR4_FCOMB::ENABLED_ON_FIFO_READS_WRITES;
    }

    return result;
}

}; // namespace MIMXRT1176::CM7
