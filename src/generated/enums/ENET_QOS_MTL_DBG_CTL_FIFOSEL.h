/**
 * \file
 * \brief Generated by ifgen (3.3.0).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace MIMXRT1176::CM7
{

enum class ENET_QOS_MTL_DBG_CTL_FIFOSEL : uint8_t
{
    TXFIFO /*!< Tx FIFO */,
    TXSTSFIFO = 1 /*!< Tx Status FIFO (only read access when SLVMOD is set) */,
    TSOFIFO = 2 /*!< TSO FIFO (cannot be accessed when SLVMOD is set) */,
    RXFIFO = 3 /*!< Rx FIFO */
};
static_assert(sizeof(ENET_QOS_MTL_DBG_CTL_FIFOSEL) == 1);

/**
 * Converts ENET_QOS_MTL_DBG_CTL_FIFOSEL to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(ENET_QOS_MTL_DBG_CTL_FIFOSEL instance)
{
    const char *result = "UNKNOWN ENET_QOS_MTL_DBG_CTL_FIFOSEL";

    switch (instance)
    {
    case ENET_QOS_MTL_DBG_CTL_FIFOSEL::TXFIFO:
        result = "TXFIFO";
        break;
    case ENET_QOS_MTL_DBG_CTL_FIFOSEL::TXSTSFIFO:
        result = "TXSTSFIFO";
        break;
    case ENET_QOS_MTL_DBG_CTL_FIFOSEL::TSOFIFO:
        result = "TSOFIFO";
        break;
    case ENET_QOS_MTL_DBG_CTL_FIFOSEL::RXFIFO:
        result = "RXFIFO";
        break;
    }

    return result;
}

/**
 * Converts a C string to ENET_QOS_MTL_DBG_CTL_FIFOSEL.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, ENET_QOS_MTL_DBG_CTL_FIFOSEL &output)
{
    bool result = false;

    if ((result = !strncmp(data, "TXFIFO", 6)))
    {
        output = ENET_QOS_MTL_DBG_CTL_FIFOSEL::TXFIFO;
    }
    else if ((result = !strncmp(data, "TXSTSFIFO", 9)))
    {
        output = ENET_QOS_MTL_DBG_CTL_FIFOSEL::TXSTSFIFO;
    }
    else if ((result = !strncmp(data, "TSOFIFO", 7)))
    {
        output = ENET_QOS_MTL_DBG_CTL_FIFOSEL::TSOFIFO;
    }
    else if ((result = !strncmp(data, "RXFIFO", 6)))
    {
        output = ENET_QOS_MTL_DBG_CTL_FIFOSEL::RXFIFO;
    }

    return result;
}

}; // namespace MIMXRT1176::CM7
