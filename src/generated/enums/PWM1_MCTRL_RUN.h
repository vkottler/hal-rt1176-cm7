/**
 * \file
 * \brief Generated by ifgen (3.3.0).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace MIMXRT1176::CM7
{

enum class PWM1_MCTRL_RUN : uint8_t
{
    DISABLED /*!< PWM counter is stopped, but PWM outputs will hold the current
                state. */
        ,
    ENABLED = 1 /*!< PWM counter is started in the corresponding submodule. */
};
static_assert(sizeof(PWM1_MCTRL_RUN) == 1);

/**
 * Converts PWM1_MCTRL_RUN to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(PWM1_MCTRL_RUN instance)
{
    const char *result = "UNKNOWN PWM1_MCTRL_RUN";

    switch (instance)
    {
    case PWM1_MCTRL_RUN::DISABLED:
        result = "DISABLED";
        break;
    case PWM1_MCTRL_RUN::ENABLED:
        result = "ENABLED";
        break;
    }

    return result;
}

/**
 * Converts a C string to PWM1_MCTRL_RUN.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, PWM1_MCTRL_RUN &output)
{
    bool result = false;

    if ((result = !strncmp(data, "DISABLED", 8)))
    {
        output = PWM1_MCTRL_RUN::DISABLED;
    }
    else if ((result = !strncmp(data, "ENABLED", 7)))
    {
        output = PWM1_MCTRL_RUN::ENABLED;
    }

    return result;
}

}; // namespace MIMXRT1176::CM7
